# GPU training dependencies (install AFTER PyTorch with CUDA — see install_gpu.sh).
# Do not add `torch` here; it is installed from the PyTorch CUDA wheel index.

transformers>=4.48
numpy>=1.24
scikit-learn>=1.3
pandas>=2.0
tqdm>=4.66
pyyaml>=6.0
pytest>=7.0

# Audio SER backend (wav2vec2 on real MELD audio)
soundfile>=0.12
librosa>=0.10

# Optional: LLM silver-label regeneration
# openai>=1.0

# Optional: Whisper ASR pipeline
# openai-whisper>=20231117
